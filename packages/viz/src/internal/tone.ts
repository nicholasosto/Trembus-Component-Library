// The viz tone vocabulary is the shared @trembus/tokens ontology — one source of
// truth for the color-coded tones across @trembus/ui (Tier-1) and @trembus/viz.
export type { Tone as VizTone } from '@trembus/tokens';
export { toneVar, toneFg } from '@trembus/tokens';
