// The shared axe a11y helper lives in @trembus/tokens/testing (one disabled-rule
// list across packages). This shim keeps ui's test import paths stable.
export { a11yViolations } from '@trembus/tokens/testing';
