// The token scale types live in @trembus/tokens. This shim preserves
// @trembus/ui's internal import paths AND its public barrel surface unchanged.
export type {
  SpaceToken,
  RadiusToken,
  TypeToken,
  FontWeightToken,
  ElevationToken,
  ZToken,
  StatusTone,
  TextTone,
  SurfaceTone,
  MaterialTone,
  ThemeName,
} from '@trembus/tokens';
